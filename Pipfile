[[source]]
name = "pypi"
url = "https://pypi.org/simple"
verify_ssl = true

[packages]
autolina-scraper = {editable = true, path = "."}

[dev-packages]
pytest = ">=8.0"
pytest-cov = ">=5.0"
responses = ">=0.25"
mypy = ">=1.10"
ruff = ">=0.6"
types-requests = "*"

[requires]
python_version = "3.13"
